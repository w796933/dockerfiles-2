#!/bin/sh
: ${HOST:=/host}

if [ -h "$0" ]; then
  /usr/sbin/chroot "$HOST" "$(basename "$0")" "$@"
elif [ $# -gt 0 ]; then
  /usr/sbin/chroot "$HOST" "$@"
else # shell ...
  /usr/sbin/chroot "$HOST" bash
fi
